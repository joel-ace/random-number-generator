import React from 'react';

const Header = () => (
	<header>
		<nav className="navbar navbar-dark bg-primary" style={{borderRadius: 0}}>
			<span className="navbar-text" style={{ fontSize: '1.5em' }}>
				Random Number Generator
			</span>
		</nav>
	</header>
);

export default Header;
